# ================================
# 域名配置
# ================================
VAULTWARDEN_HOST=vault.example.com

# ================================
# Admin 页面
# ================================
# 通过 init.sh 自动生成，或手动设置
# 生成方式: openssl rand -base64 48
ADMIN_TOKEN=change_me_to_a_secure_token

# ================================
# SMTP 邮件配置
# ================================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURITY=starttls
SMTP_USERNAME=your@email.com
SMTP_PASSWORD=your_smtp_password
SMTP_FROM=vault@example.com

# ================================
# S3 备份配置 (用于 backup.sh)
# ================================
# AWS S3 或兼容 S3 的存储 (如 Cloudflare R2, MinIO)
S3_ENDPOINT=https://s3.amazonaws.com
S3_BUCKET=your-backup-bucket
S3_ACCESS_KEY=your_access_key
S3_SECRET_KEY=your_secret_key
S3_REGION=us-east-1

# ================================
# 远程服务器备份配置 (用于 backup.sh)
# ================================
# SSH 方式备份到远程服务器
REMOTE_HOST=backup.example.com
REMOTE_USER=backup
REMOTE_PATH=/home/backup/vaultwarden
# SSH 密钥路径 (相对于 scripts 目录或绝对路径)
REMOTE_SSH_KEY=~/.ssh/id_rsa

# ================================
# 备份验证配置 (用于 verify-backup.sh)
# ================================
# 创建一个测试账号和测试密码条目用于验证
VERIFY_TEST_EMAIL=test@example.com
VERIFY_TEST_PASSWORD=your_test_master_password
VERIFY_TEST_ITEM=backup-test
VERIFY_EXPECTED_VALUE=expected_username_or_note
VERIFY_SANDBOX_PORT=18080
# 每次备份后验证最近 n 个备份 (0 = 不验证)
VERIFY_BACKUP_COUNT=3

# ================================
# 时区
# ================================
TZ=Asia/Shanghai
